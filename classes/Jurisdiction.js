/**
 * Class for the jurisdiction.
 */
export default class Jurisdiction {
  constructor(id, name, roles, rules) {
    this.id = id;
    this.name = name;
    this.roles = roles;
    this.rules = rules;
  }
}
