/**
 * Class for metadata of action.
 */
export default class ActionMetadata {
  constructor(name, description, icon) {
    this.name = name;
    this.description = description;
    this.icon = icon;
  }
}
